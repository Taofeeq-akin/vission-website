var typed = new Typed(".typed", {
  // Waits 1000ms after typing "First"
  strings: ["PROGRAMMING.", "JAVASCRIPT."],
});

// import Typed from "typed.js";

// var options = {
//   strings: ["<i>First</i> sentence.", "&amp; a second sentence."],
//   typeSpeed: 40,
// };

// var typed = new Typed(".", options);
